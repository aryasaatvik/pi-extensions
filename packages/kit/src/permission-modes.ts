// Preset entry: re-exports the @pi-ext/permission-modes extension factory so this
// one package wires up permission modes when installed. See package.json `pi.extensions`.
export { default } from "@pi-ext/permission-modes";
